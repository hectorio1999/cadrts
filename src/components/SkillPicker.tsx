// Deprecated: the inline skill picker was replaced by the full SkillLibrary
// modal (browse by category, search, detail views, example prompts). This
// re-export keeps any lingering import resolving to the new component.
export { default } from "./SkillLibrary";
